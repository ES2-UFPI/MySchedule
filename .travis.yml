language: node_js
install:
  - npm install 
  - npm ci
node_js:
  - "stable"
cache:
  directories:
  - node_modules

branches:
  only:
    - master1
    - dev
script:
  - npm test
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $github-token  # Set in travis-ci.org dashboard, marked secure
  local_dir: build
  keep-history: true
  on:
    branch: master

